package com.ajemian.cs175homework1;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

import java.util.HashMap;

public class NutritionManager extends AppCompatActivity
{
    //grams
    /*
    private int protein = 0;
    private int carbs = 0;
    private int alcohol = 0;
    private int fat = 0;
    private int calories = 0;

    public NutritionManager()
    {
        if(tomatoCount > 0)
        {
            calories += numOfTomatos * 22 cal;
        }
        if(eggCount > 0)
        {
            protein += numberOfEggs * 6;
            fat += numberOfEggs * 5;
            calories += numOfTomatos * 78 cal;
        }
        if(Flour > 0)
        {
            protein += numberOfEggs * 13;
            fat += numberOfEggs * 1;
            carbs += numberOfFlour * 95;
            calories += numOfTomatos * 465 cal;
        }
        <item>Chicken</item>
        <item>Olive Oil</item>
        <item>Yogurt</item>
        <item>Beef</item>
        <item>Potatoes</item>
        <item>Pork</item>
    }
*/
    @Override
    protected void onCreate(Bundle savedInstanceState)
    {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_nutrition_manager);
    }
}
